<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/menu_right"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="right"
    android:orientation="vertical" >

    <include layout="@layout/top_header"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:background="@drawable/edittext_back" >

        <EditText
            android:id="@+id/et_serch_right_side_menu"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:drawablePadding="5dp"
            android:drawableRight="@drawable/ic_action_search"
            android:hint="Search"
            android:padding="5dp"
            android:textColor="@color/white"
            android:textColorHint="@color/white_light" />

        <!-- <ImageView -->
        <!-- android:id="@+id/btn_serch_right_side" -->
        <!-- android:layout_width="wrap_content" -->
        <!-- android:layout_height="wrap_content" -->
        <!-- android:src="@android:drawable/ic_search_category_default" /> -->
    </LinearLayout>

    <RelativeLayout
        android:id="@+id/macheslistviewlayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="right|top"
        android:layout_marginTop="5dp" >

        <ListView
            android:id="@+id/menu_right_ListView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:cacheColorHint="#00000000"
            android:listSelector="@drawable/selector_transparent_clicked"
            android:divider="@color/transparent" >
        </ListView>
    </RelativeLayout>

</LinearLayout>